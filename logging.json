{
  "templates": {
    "source": "${name}:${line}",
    "default": "[${level}] ${function} (${source}) ${text}",
    "FATAL": "[${level}] (${name}) ${stack}"
  },
  "display": {
    "default": true,
    "DEBUG": true
  }
}
