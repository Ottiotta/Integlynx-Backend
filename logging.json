{
  "templates": {
    "source": "${name}:${line}",
    "default": "[${level}] ${function} (${source}) ${text}",
    "CATCH-ERROR": "[ERROR] ${name} (${message}) ${text}",
    "FATAL": "[${level}] ${name} (${message}) ${text}"
  },
  "display": {
    "default": true,
    "DEBUG": true
  }
}
